# Phase 7: SEO, Accessibility & Custom 404

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add comprehensive SEO metadata, accessibility improvements, sitemap/robots, JSON-LD structured data, and a bilingual custom 404 page.

**Architecture:** Use Next.js App Router metadata API (`generateMetadata`, `sitemap.ts`, `robots.ts`) for SEO. Add skip-to-content and ARIA improvements inline. Create locale-aware `not-found.tsx`. Convert the one `<img>` tag to `next/image`.

**Tech Stack:** Next.js 16 metadata API, next-intl, next/image

---

### Task 1: Root Layout — Fix Dynamic `lang` Attribute

**Files:**
- Modify: `src/app/layout.tsx`

**Step 1:** The root `<html lang="en">` is hardcoded. Since the locale layout handles i18n, update root layout to not hardcode `lang` — it will be set by the locale layout.

Remove `lang="en"` from the `<html>` tag (keep `suppressHydrationWarning`).

**Step 2:** Modify `src/app/[locale]/layout.tsx` to pass `lang` to the `<html>` element. Since Next.js App Router nests layouts and root layout owns `<html>`, we instead set the lang via a small client component or accept that root layout can't know the locale. The pragmatic fix: keep `lang="en"` as default in root, and add `<script>` or metadata approach.

Actually, the cleanest approach: move font/body logic into locale layout and have root layout be minimal, OR accept the root layout `lang="en"` and override per-page via metadata. For this project, the simplest fix is to leave root as-is and ensure `generateMetadata` sets the correct `alternates.languages`.

**Decision:** Keep root layout `lang="en"` as fallback. The locale layout will set metadata with correct locale info. Skip this task — not worth the complexity.

---

### Task 2: Per-Page SEO Metadata — Home Page

**Files:**
- Modify: `src/app/[locale]/page.tsx`

**Step 1:** Add `generateMetadata` to the home page with title, description, Open Graph, and alternate locale links.

```tsx
import type { Metadata } from 'next';
import { getTranslations } from 'next-intl/server';

type Props = { params: Promise<{ locale: string }> };

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: 'hero' });
  const baseUrl = 'https://masjidrahma.no';

  return {
    title: `Masjid Rahma Oslo — ${t('subtitle')}`,
    description: t('subtitle'),
    alternates: {
      canonical: `${baseUrl}/${locale}`,
      languages: { en: `${baseUrl}/en`, no: `${baseUrl}/no` },
    },
    openGraph: {
      title: 'Masjid Rahma Oslo',
      description: t('subtitle'),
      url: `${baseUrl}/${locale}`,
      siteName: 'Masjid Rahma',
      locale: locale === 'no' ? 'nb_NO' : 'en_US',
      type: 'website',
    },
  };
}
```

**Step 2:** Verify the dev server starts without errors: `npm run dev`

---

### Task 3: Per-Page SEO Metadata — Donate Page

**Files:**
- Modify: `src/app/[locale]/donate/page.tsx`

**Step 1:** Add `generateMetadata` with donate-specific title/description.

```tsx
import type { Metadata } from 'next';
import { getTranslations } from 'next-intl/server';

type Props = { params: Promise<{ locale: string }> };

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: 'donate' });
  const baseUrl = 'https://masjidrahma.no';

  return {
    title: `${t('sectionTitle')} — Masjid Rahma`,
    description: t('pageSubtitle'),
    alternates: {
      canonical: `${baseUrl}/${locale}/donate`,
      languages: { en: `${baseUrl}/en/donate`, no: `${baseUrl}/no/donate` },
    },
    openGraph: {
      title: `${t('sectionTitle')} — Masjid Rahma`,
      description: t('pageSubtitle'),
      url: `${baseUrl}/${locale}/donate`,
      siteName: 'Masjid Rahma',
      locale: locale === 'no' ? 'nb_NO' : 'en_US',
      type: 'website',
    },
  };
}
```

---

### Task 4: Per-Page SEO Metadata — Contact Page

**Files:**
- Modify: `src/app/[locale]/contact/page.tsx`

**Step 1:** Add `generateMetadata`.

```tsx
import type { Metadata } from 'next';
import { getTranslations } from 'next-intl/server';

type Props = { params: Promise<{ locale: string }> };

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: 'contact' });
  const baseUrl = 'https://masjidrahma.no';

  return {
    title: `${t('directInquiry')} — Masjid Rahma`,
    description: t('visitDesc'),
    alternates: {
      canonical: `${baseUrl}/${locale}/contact`,
      languages: { en: `${baseUrl}/en/contact`, no: `${baseUrl}/no/contact` },
    },
    openGraph: {
      title: `${t('directInquiry')} — Masjid Rahma`,
      description: t('visitDesc'),
      url: `${baseUrl}/${locale}/contact`,
      siteName: 'Masjid Rahma',
      locale: locale === 'no' ? 'nb_NO' : 'en_US',
      type: 'website',
    },
  };
}
```

---

### Task 5: Per-Page SEO Metadata — Heritage Page

**Files:**
- Modify: `src/app/[locale]/heritage/page.tsx`

**Step 1:** Add `generateMetadata`.

```tsx
import type { Metadata } from 'next';
import { getTranslations } from 'next-intl/server';

type Props = { params: Promise<{ locale: string }> };

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: 'heritage' });
  const baseUrl = 'https://masjidrahma.no';

  return {
    title: `${t('pageTitle')} — Masjid Rahma`,
    description: t('foundation.desc'),
    alternates: {
      canonical: `${baseUrl}/${locale}/heritage`,
      languages: { en: `${baseUrl}/en/heritage`, no: `${baseUrl}/no/heritage` },
    },
    openGraph: {
      title: `${t('pageTitle')} — Masjid Rahma`,
      description: t('foundation.desc'),
      url: `${baseUrl}/${locale}/heritage`,
      siteName: 'Masjid Rahma',
      locale: locale === 'no' ? 'nb_NO' : 'en_US',
      type: 'website',
    },
  };
}
```

---

### Task 6: Sitemap

**Files:**
- Create: `src/app/sitemap.ts`

**Step 1:** Create dynamic sitemap covering all locale/page combos.

```ts
import type { MetadataRoute } from 'next';

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = 'https://masjidrahma.no';
  const locales = ['en', 'no'];
  const pages = ['', '/donate', '/contact', '/heritage'];

  return locales.flatMap((locale) =>
    pages.map((page) => ({
      url: `${baseUrl}/${locale}${page}`,
      lastModified: new Date(),
      changeFrequency: page === '' ? 'daily' as const : 'weekly' as const,
      priority: page === '' ? 1 : 0.8,
    }))
  );
}
```

---

### Task 7: Robots.txt

**Files:**
- Create: `src/app/robots.ts`

**Step 1:** Create robots config.

```ts
import type { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  const baseUrl = 'https://masjidrahma.no';

  return {
    rules: {
      userAgent: '*',
      allow: '/',
    },
    sitemap: `${baseUrl}/sitemap.xml`,
  };
}
```

---

### Task 8: JSON-LD Structured Data (Mosque Schema)

**Files:**
- Modify: `src/app/[locale]/page.tsx`

**Step 1:** Add JSON-LD script to the home page for `Mosque` schema.

```tsx
function MosqueJsonLd() {
  const jsonLd = {
    '@context': 'https://schema.org',
    '@type': 'Mosque',
    name: 'Masjid Rahma',
    alternateName: 'Masjid Rahma Oslo',
    url: 'https://masjidrahma.no',
    telephone: '+47 22 12 34 56',
    email: 'contact@masjidrahma.no',
    address: {
      '@type': 'PostalAddress',
      streetAddress: 'Gronland 12',
      addressLocality: 'Oslo',
      postalCode: '0188',
      addressCountry: 'NO',
    },
    geo: {
      '@type': 'GeoCoordinates',
      latitude: 59.9127,
      longitude: 10.7601,
    },
    openingHoursSpecification: {
      '@type': 'OpeningHoursSpecification',
      dayOfWeek: ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'],
      opens: '05:00',
      closes: '23:00',
    },
  };

  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
    />
  );
}
```

Add `<MosqueJsonLd />` inside the home page's fragment.

---

### Task 9: Skip-to-Content & Main Landmark

**Files:**
- Modify: `src/app/[locale]/layout.tsx`

**Step 1:** Add a skip-to-content link before the Navbar and add `id="main-content"` to `<main>`.

```tsx
<NextIntlClientProvider locale={locale} messages={messages}>
  <a
    href="#main-content"
    className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-[100] focus:px-4 focus:py-2 focus:bg-primary focus:text-background-dark focus:rounded-lg focus:font-semibold"
  >
    Skip to content
  </a>
  <Navbar />
  <main id="main-content">{children}</main>
  <Footer />
</NextIntlClientProvider>
```

---

### Task 10: Accessibility — LanguageSwitcher

**Files:**
- Modify: `src/components/layout/LanguageSwitcher.tsx`

**Step 1:** Change `<span>` elements to `<button>` elements with proper `aria-label` and `aria-current`.

Replace each `<span onClick=...>` with:
```tsx
<button
  onClick={() => switchLocale('en')}
  aria-label="Switch to English"
  aria-current={locale === 'en' ? 'true' : undefined}
  className={...}
>
  EN
</button>
```

Same for the NO button with `aria-label="Bytt til norsk"`.

---

### Task 11: Accessibility — DonationWidget Radio Labels

**Files:**
- Modify: `src/components/donate/DonationWidget.tsx`

**Step 1:** The disabled radio labels (Apple Pay, Vipps) have `cursor-not-allowed` but no `aria-disabled`. Add `aria-disabled="true"` to the parent `<label>` elements for Apple Pay and Vipps.

---

### Task 12: Image Optimization — Donate Page Map

**Files:**
- Modify: `src/app/[locale]/donate/page.tsx`

**Step 1:** Replace the raw `<img>` tag on line 128 with `next/image`:

```tsx
import Image from 'next/image';

// Replace the <img> with:
<Image
  src="https://lh3.googleusercontent.com/aida-public/AB6AXuCcOzxfsgr03W5MbrkOV1I6rCyO6qMwm5LhCfwTDfXJqwC-xDI73cmf_duO_ua5SjtqAwKvmvNboZVkmKi1AHfBXl_TI-pQTtPlorhuVnhjMSfjABwdmVLTtubG7Xp2zVc-5ujMBPSV8VrUgTTxvdgjr5RUdKbxC3Ax6qVIMyIH80SSZdOnqsMBlbUnThFg2XhEPmYlubuf0GWnlHQ2LWf6owlZzPlIEJY3jnSPzM2xL7P4Pg0Sfc0mRdL0kzpxtyZ9nCYsqde1e1Y"
  alt="Map showing Masjid Rahma location in Oslo"
  fill
  className="object-cover grayscale opacity-30 group-hover:grayscale-0 group-hover:opacity-50 transition-all duration-700"
/>
```

---

### Task 13: Custom 404 Page

**Files:**
- Create: `src/app/[locale]/not-found.tsx`

**Step 1:** Create a bilingual 404 page styled consistently with the site.

```tsx
import { Link } from '@/i18n/navigation';

export default function NotFound() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="text-center px-4">
        <div className="mb-8">
          <span className="material-icons text-primary text-7xl">mosque</span>
        </div>
        <h1 className="text-8xl font-bold text-primary mb-4 font-[family-name:var(--font-display)]">
          404
        </h1>
        <p className="text-xl text-[var(--color-text-muted)] mb-8 max-w-md mx-auto">
          The page you are looking for does not exist or has been moved.
        </p>
        <Link
          href="/"
          className="inline-flex items-center gap-2 px-8 py-4 bg-primary text-background-dark font-semibold rounded-full hover:opacity-90 transition-opacity"
        >
          <span className="material-icons">home</span>
          Go Home
        </Link>
      </div>
    </main>
  );
}
```

---

### Task 14: Add 404 Translation Keys

**Files:**
- Modify: `src/i18n/messages/en.json`
- Modify: `src/i18n/messages/no.json`

**Step 1:** Add `notFound` keys to both locale files.

English:
```json
"notFound": {
  "title": "404",
  "message": "The page you are looking for does not exist or has been moved.",
  "goHome": "Go Home"
}
```

Norwegian:
```json
"notFound": {
  "title": "404",
  "message": "Siden du leter etter finnes ikke eller er flyttet.",
  "goHome": "Gå hjem"
}
```

Then update `not-found.tsx` to use translations.

---

### Task 15: Verify Build

**Step 1:** Run `npm run build` to verify everything compiles cleanly.

**Expected:** Build succeeds with no errors.
